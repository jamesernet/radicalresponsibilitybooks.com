/* assets/images/README.md

Place these image files here before running `npm start`:

  lee-hero.jpg    — Hero portrait (tall, strong energy photo)
                    Recommended: 800×1000px minimum
                    Used in: hero section (right column, 680px height)

  lee-about.jpg   — About section portrait (warmer, approachable)
                    Recommended: 600×750px minimum
                    Used in: author section (left column, 580px height)

  book-cover.jpg  — Book cover image
                    Recommended: 500×700px minimum
                    Used in: book section (left column, full width)

  og-image.jpg    — Social sharing image
                    REQUIRED SIZE: exactly 1200×630px
                    Used in: Open Graph / Twitter Card meta tags
                    This is what appears when someone shares the URL on
                    social media. Make it count — include title + author.

  favicon.ico     — Browser tab icon
                    16×16 and 32×32 (multi-resolution .ico file)

All images go in this directory: assets/images/
They are referenced in src/_data/site.json with paths like /assets/images/filename.jpg

*/
